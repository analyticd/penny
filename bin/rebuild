#!/bin/sh

set -e

build() {
    runhaskell Setup clean
    runhaskell Setup configure --user
    runhaskell Setup build
    runhaskell Setup install
    runhaskell Setup haddock --hyperlink-source --executables
}

( cd ../lib && build )
build

