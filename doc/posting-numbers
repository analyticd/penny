transaction, for the entire transaction. On the TopLineMeta.
unfiltered, after transactions have been split. On the PostingMeta.
filtered, after filtering. On the PostingBox.
sorted, after sorting. On the PostingBox.
visible, visible postings only. On the PostingBox.
