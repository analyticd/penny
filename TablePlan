[PostingBox]

<<Compute balances>>

[(PostingBox, Balance)]

<<Number Postings>>

[(PostingBox, Balance, PostingNum, RevPostingNum)]

<<Filter, leaving visible postings only>>

[(PostingBox, Balance, PostingNum, RevPostingNum)]

<<Add VisibleNum>>

[(PostingBox, Balance, PostingNum, RevPostingNum, VisibleNum)]

<<Multiply into tranches>>

[(PostingBox, Balance, PostingNum, RevPostingNum, VisibleNum,
  TrancheRow)]

<<To Array>>

Array (Column, (VisibleNum, TrancheRow)) PostingInfo

<<Space Claim>>

Array (Column, (VisibleNum, TrancheRow)) (PostingInfo, Maybe Width)

<<Allocate SpaceToFit>>

Array (Column, (VisibleNum, TrancheRow)) (PostingInfo, Maybe Cell)

<<Allocate Allocations>>

Array (Column, (VisibleNum, TrancheRow)) (PostingInfo, Cell)
